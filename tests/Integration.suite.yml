# Codeception Test Suite Configuration
#
# Suite for integration tests
# Emulate web requests and make application process them
# Include one of framework modules (Symfony, Yii2, Laravel, Phalcon5) to use it
# Remove this suite if you don't use frameworks

actor: IntegrationTester
suite_namespace: Tests\Integration
bootstrap: false
modules:
    enabled:
        - Asserts
        - \Tests\Support\Helper\Integration
        - lucatume\WPBrowser\Module\WPLoader   #负责加载 WordPress 核心、安装、初始化环境
        - lucatume\WPBrowser\Module\WPDb       #负责数据库管理、创建文章/用户/元数据等
    config:
          lucatume\WPBrowser\Module\WPLoader:
              wpRootFolder: '/var/www/html/wp/'
              dbUrl: 'mysql://root:H0a9S1h8@127.0.0.1:3306/test_db'
              domain: 'wordpress682.local'
              adminUsername: 'admin'
              adminPassword: '^H0a9S1h8$~'
              adminEmail: 'robert2021south@gmail.com'
              loadOnly: true
          lucatume\WPBrowser\Module\WPDb:
              dsn: 'mysql:host=127.0.0.1;dbname=test_db'
              user: 'root'
              password: 'H0a9S1h8'
              dump: ''
              populate: false
              cleanup: false
              url: 'http://wordpress682.local'
              tablePrefix: 'wp_'
step_decorators: ~
